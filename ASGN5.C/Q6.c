#include<stdio.h>
int main(){
int a;
printf("enter a numer!");
scanf("%d",&a);
if(a>=100,a<1000)
{
printf("given number is a three digit number ");
}
else{ 
printf("not a three digit number");
}
    return 0;
}